import { useState } from 'react'
import Head from 'next/head'
import Image from 'next/image'
import Layout from "../../components/Layout"
import ButtonGoHome from "../../components/ButtonGoHome"
import FirstParagraph from "../../components/EvolutionComponent/FirstParagraph"
import SecondParagraph from "../../components/EvolutionComponent/SecondParagraph"
import ThirdParagraph from "../../components/EvolutionComponent/ThirdParagraph"
import FourthParagraph from "../../components/EvolutionComponent/FourthParagraph"
import FifthParagraph from "../../components/EvolutionComponent/FifthParagraph"
import BtnArrow from "../../components/EvolutionComponent/BtnArrow"
import { BsFillArrowRightCircleFill } from 'react-icons/bs'
import styles from '@/styles/Evolution.module.scss'

const Evolution:React.FC = () => {

  const [first, setFirst] = useState<boolean>(true)
  const [second, setSecond] = useState<boolean>(true)
  const [third, setThird] = useState<boolean>(true)
  const [four, setFour] = useState<boolean>(true)
  const [five, setFive] = useState<boolean>(true)

  const handleFirst = () => {
    setFirst(first => !first)
  }

  const handleSecond = () => {
    setSecond(second => !second)
  }

  const handleThird = () => {
    setThird(third => !third)
  }

  const handleFour = () => {
    setFour(four => !four)
  }

  const handleFive = () => {
    setFive(five => !five)
  }

  return (
    <Layout>
    
      <Head>
        <title>Evolution</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <div className={styles.evolution}>

        <ButtonGoHome />

        <div className={styles.imgabout}>
          <Image
            src={'/coolnection.jpg'}
            width="1680"
            height="1200"
            alt="img about"
            className={styles.img}
          />
        </div>

        <h1 className={styles.mainheaderevolution}>Evolution</h1>

        <article className={styles.article}>
          

            <div className={styles.listevolution}>

            {first === false ? (
              <BtnArrow boolChange={first} handleChangeFunc={handleFirst} />
              ) : (
              <BtnArrow boolChange={first} handleChangeFunc={handleFirst} />
              )
            }

            <a className={styles.evolution_a}>
              2017-2018
            </a>
          </div>

          {first === true ? (
            <FirstParagraph boolStyles={first}/>
            ) : (
            <FirstParagraph boolStyles={first}/>
            )
          }


          <hr className={styles.hr_evolution}/>
          <br />

          <div className={styles.listevolution}>

            {second === false ? (
              <BtnArrow boolChange={second} handleChangeFunc={handleSecond} />
              ) : (
              <BtnArrow boolChange={second} handleChangeFunc={handleSecond} />
              )
            }

            <a className={styles.evolution_a}>
              2019-2020
            </a>
          </div>

          {second === true ? (
            <SecondParagraph boolStyles={second} />
            ) : (
            <SecondParagraph boolStyles={second} />
            )
          }

          <hr className={styles.hr_evolution}/>
          <br />
          
          <div className={styles.listevolution}>

            {third === false ? (
              <BtnArrow boolChange={third} handleChangeFunc={handleThird} />
              ) : (
              <BtnArrow boolChange={third} handleChangeFunc={handleThird} />
              )
            }

            <a className={styles.evolution_a}>
              2020-2021
            </a>
          </div>

          {third === true ? (
            <ThirdParagraph boolStyles={third} />
            ) : (
            <ThirdParagraph boolStyles={third} />
            )
          }


          <hr className={styles.hr_evolution}/>
          <br />
          
          <div className={styles.listevolution}>

            {four === false ? (
              <BtnArrow boolChange={four} handleChangeFunc={handleFour} />
              ) : (
              <BtnArrow boolChange={four} handleChangeFunc={handleFour} />
              )
            }

            <a className={styles.evolution_a}>
              2021-2023
            </a>
          </div>

          {four === true ? (
            <FourthParagraph boolStyles={four} />
            ) : (
            <FourthParagraph boolStyles={four} />
            )
          }

          <hr className={styles.hr_evolution}/>
          <br />
          
          <div className={styles.listevolution}>

            {five === false ? (
              <BtnArrow boolChange={five} handleChangeFunc={handleFive} />
              ) : (
              <BtnArrow boolChange={five} handleChangeFunc={handleFive} />
              )
            }

            <a className={styles.evolution_a}>
              2023 
            </a>
          </div>
          
          {five === true ? (
            <FifthParagraph boolStyles={five} />
            ) : (
            <FifthParagraph boolStyles={five} />
            )
          }

          <hr
            className={styles.hr_evolution}
            style={{marginBottom: "0px"}} />
          <br />
        </article>

      </div>
    </Layout>
  )
}
export default Evolution